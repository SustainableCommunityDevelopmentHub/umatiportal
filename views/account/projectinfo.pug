extends ../layout

block content
  .pb-2.mt-2.mb-4.border-bottom

 
  - var userr = { id: user.group }
  if userr.id  === ""
         span.caps unspecified project - 
  else
         span.caps #{ user.group } Project Information Form-
  a(href='/account/projectinfosettings') Settings

    hr

    br

  - if (projectinfo.length)
    each items in projectinfo
         if items.project === userr.id 
           strong #{items.name} #{items.group} #{items.date}
           div admin: #{items.admin} witness: #{items.witness}
           div #{items.comment}
           hr

  br
  br
  br
  br
  br
  br
  hr

  include ../partials/profilehead.pug
